# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Provider abstraction | IssueProvider interface with GitLab/GitHub implementations | Both providers used daily, need first-class support for both | No |
| D002 | M001 | arch | Provider auto-detection | Detect from git remote URL (gitlab.com → GitLab, github.com → GitHub) | Matches predecessor pattern, zero-config for common case | Yes — if self-hosted instances need custom detection |
| D003 | M001 | convention | Config file | Unified .gsd/issues.json with provider-specific sections | User confirmed — one config file, not separate per-provider | No |
| D004 | M001 | convention | Mapping file format | ISSUE-MAP.json (clean break from GITLAB-MAP.json) | User confirmed new format only, no backward compat | No |
| D005 | M001 | arch | Import workflow design | Extension fetches/formats, LLM interprets via sendUserMessage | Import is inherently fuzzy/creative work — LLM judgment, not deterministic pipeline | No |
| D006 | M001 | arch | Sync trigger model | Prompted step in GSD flow (not manual-only, not auto) | User correction: should be part of the flow but with explicit confirmation before creating remote issues | No |
| D007 | M001 | arch | Lifecycle hook for close | Watch tool_result for S##-SUMMARY.md writes | More precise than watching agent_end — triggers on exact file write signaling completion | Yes — if a dedicated slice completion event is added to GSD core |
| D008 | M001 | arch | Event bus usage | Emit gsd-issues:* events on pi.events for composability | Cheap to add, enables future extension interop | No |
| D009 | M001 | scope | GitLab extras | Full support: epics, weight (S/M/L), T::Done labels, reorg | User confirmed — these are actively used, not optional metadata | No |
| D010 | M001 | scope | npm distribution | Core requirement, not deferred | User confirmed — must be installable and updatable | No |
| D011 | M001/S01 | scope | Defer state helpers to S03 | `readGSDState()` and `parseRoadmapSlices()` built in S03 when sync needs them, not S01 | These are simple file-reading utilities only consumed by sync — building them without a consumer to test against adds dead code to S01 | Yes — pull into S01 if S03 planning reveals a need |
| D012 | M001/S01 | arch | Injected exec function for testability | Providers take an `ExecFn` parameter instead of importing `pi.exec()` directly | Enables mock-based testing without real CLIs, and decouples library code from pi runtime | No |
| D013 | M001/S01 | convention | Provider state normalization | GitLab `opened` → `open`, GitHub `OPEN`/`CLOSED` → lowercase | Downstream code always sees consistent lowercase `open`/`closed` regardless of provider | No |
| D014 | M001/S01 | convention | Structural validation over schema libraries | IssueMapEntry validation checks field names and types directly, no runtime schema dep | Zero runtime dependencies in the library layer — validation is explicit and debuggable | Yes — if a schema library is introduced for config validation in S02 |
| D015 | M001/S02 | arch | Extension API types defined locally | Minimal ExtensionAPI/ExtensionCommandContext/ExtensionUI interfaces in src/index.ts | No @gsd/pi-coding-agent package dependency available — types match the pi extension contract shape observed in GSD core extension | Yes — if pi exposes a public types package |
| D016 | M001/S02 | convention | Interactive command pattern | guard ctx.hasUI → detect → discover → collect → assemble → save → validate → summarize | Consistent flow for any future interactive commands; hasUI guard prevents runtime errors in non-interactive contexts | No |
| D017 | M001/S02 | convention | Default done_label per provider | "T::Done" for GitLab, empty string for GitHub | Matches existing GitLab project convention; GitHub has no standard done label | Yes — if GitHub projects adopt a label convention |
| D018 | M001/S03 | arch | Epic discovery at sync time | Discover GitLab group path via `glab api "projects/{id}"` at sync time rather than extending GitLabConfig schema | Avoids breaking S02's config shape; epic config stays as `"&42"` string; group info is discoverable from the project | Yes — if epic assignment needs to work offline or faster |
| D019 | M001/S03 | arch | Weight defaults hardcoded | fibonacci: risk-based 1/2/3/5/8, linear: 1/2/3; no per-slice size prompt | Config has `weight_strategy` but no size values — hardcoded defaults avoid blocking sync on missing config; can extend config later | Yes — if per-slice weight selection is needed |
| D020 | M001/S03 | arch | Crash-safe map writes | ISSUE-MAP.json saved after each issue creation, not batch-at-end | If sync crashes mid-way, already-created issues are in the map and won't be duplicated on re-run | No |
| D021 | M001/S03 | arch | Epic assignment best-effort | Epic assignment failure logs warning but doesn't fail the sync | Epic is metadata enrichment — the issue is already created; blocking sync on a REST API flake is worse than missing epic assignment | Yes — if epic is made mandatory |
| D022 | M001/S03 | arch | Tool mode skips confirmation | `gsd_issues_sync` tool runs sync without user confirmation; `/issues sync` command requires explicit confirm | The LLM is acting on user intent — adding a confirmation step in tool mode would break the agent workflow. Command mode has a human in the loop. | No |
| D023 | M001/S03 | arch | Provider created from config at call site | `createProvider(config, exec)` factory in both index.ts and commands/sync.ts rather than a shared module | Simple branching on `config.provider` — not worth extracting to a separate module until a third consumer appears | Yes — extract if more consumers need it |
| D024 | M001/S05 | arch | Optional fields on Issue type | Added weight?, milestone?, assignee?, description? as optional — not required | Backward-compatible with all existing consumers (sync, close). No migration needed. | No |
| D025 | M001/S05 | convention | First assignee only | Import shows assignees[0] only, ignoring additional assignees | Matches the single-assignee config pattern used throughout the extension | Yes — if multi-assignee display is needed |
| D026 | M001/S05 | convention | Import defaults to open issues | IssueFilter.state defaults to 'open' in command handler | Import is primarily for fetching active work for planning — closed issues are noise | Yes — tool path exposes state param for LLM callers who need different filtering |
| D027 | M001/S05 | arch | Tool richer than command | gsd_issues_import tool accepts state/assignee params beyond what /issues import command parses | LLM callers benefit from richer filtering; command keeps a simpler UX with just --milestone and --labels | No |
| D028 | M001/S06 | arch | registerTool single-arg API | Updated from two-arg `registerTool("name", {...})` to single-arg `registerTool({name, label, ...})` with 5-arg execute | Matches pi's real ToolDefinition contract — two-arg pattern would cause runtime crash at extension load | No |
| D029 | M002/S01 | convention | IssueMapEntry.localId holds milestone ID | No code change to issue-map module — `localId` is already a string. S02 writes milestone IDs (e.g. "M001") instead of slice IDs. | The mapping format is provider-agnostic and the field is untyped string — the semantic shift is a usage convention, not a schema change | No |
| D030 | M002/S01 | arch | createPR body includes Closes #N | `createPR()` appends `Closes #N` to the PR body when `closesIssueId` is provided | Platform-native close-on-merge behavior — no lifecycle hook needed. Both GitHub and GitLab support this syntax. | No |
| D031 | M002/S02 | arch | PR target branch resolution | Target branch resolved as: explicit param > `readIntegrationBranch()` reads META.json for source, target defaults to `main` unless overridden | Keeps Config schema unchanged — `main` default is safe for most workflows. Explicit param on tool/command allows override without config change. | Yes — if `target_branch` config field is needed |
| D032 | M002/S02 | arch | tool_result hook removal | Remove auto-close hook entirely rather than updating it | Close is now PR-driven via `Closes #N` — the hook's purpose (auto-close on slice summary write) no longer applies. Manual `/issues close` remains as fallback. Supersedes D007. | No |
| D033 | M002/S02 | arch | Milestone issue weight from highest-risk slice | Weight computed from highest-risk slice in ROADMAP.md, not per-slice individual risk | A single milestone issue represents the whole milestone — using the highest risk gives conservative weight for project planning | Yes — if per-milestone risk config is added |
| D034 | M002/S02 | arch | Milestone issue description composition | Description built from CONTEXT.md body (Project Description onward) + ROADMAP.md slice listing + gsd metadata tag | Gives the tracker issue enough context for standalone review without requiring GSD filesystem access | No |
| D035 | M002/S02 | arch | ExtensionAPI.on() removed from interface | `ToolResultEvent` type and `pi.on("tool_result", ...)` method removed from ExtensionAPI contract, not just unused | Clean break — downstream tests and future extensions won't accidentally depend on a hook mechanism that no longer exists. Follows from D032. | No |
| D036 | M002/S03 | arch | rescopeIssues calls syncMilestoneToIssue directly | Re-scope creates milestone issue by calling `syncMilestoneToIssue()` rather than duplicating create logic | Sync already handles description building, ISSUE-MAP persistence, epic assignment, crash-safe writes. Duplicating would mean two code paths to maintain. Options align — re-scope just adds the "close originals" step around it. | No |
| D037 | M002/S03 | arch | createProvider extracted to lib/provider-factory.ts | Extract from 5 inline copies to shared module. Supersedes D023. | D023 said "extract if more consumers need it" — we're at 5 identical copies. TypeScript catches any missed import updates. | No |
| D038 | M003/S01 | arch | validateMilestoneSize does its own file I/O | Function reads roadmap file internally rather than taking content as param | Matches sync.ts pattern where `syncMilestoneToIssue()` reads inline. S02 callers get a single-call API: `validateMilestoneSize(cwd, milestoneId, config)`. Tests use temp dirs (same pattern as config.test.ts). | Yes — if pure-function variant needed for different contexts |
| D039 | M003/S01 | convention | sizing_mode defaults to best_try | When `config.sizing_mode` is absent, `validateMilestoneSize()` treats it as "best_try" | Safe default — strict mode is opt-in. Oversized milestones warn but don't block unless explicitly configured. | No |
| D040 | M003/S01 | convention | Sizing fields always written to config | max_slices_per_milestone and sizing_mode always included in config (not conditional on empty) since they have defaults (5 / best_try) | Avoids ambiguity between "not set" and "default" — downstream code can always read the value | No |
| D041 | M003/S02 | arch | Orchestration as injected-dependency state machine | `AutoDeps` interface carries all external dependencies (sendMessage, newSession, waitForIdle, validateMilestoneSize, file I/O) — orchestration logic is pure functions over deps | Full testability with mocks. Same pattern as S01's ExecFn injection (D012). Auto.ts has zero direct imports of pi runtime or node:fs. | No |
| D042 | M003/S02 | arch | Separate lock files for mutual exclusion | gsd-issues writes `.gsd/issues-auto.lock` (own lock) and reads `.gsd/auto.lock` (GSD auto lock) — both use PID liveness checks | Filesystem-based mutual exclusion without shared memory or IPC. gsd-issues can't import from GSD core (separate npm package). Follows crash-recovery.ts pattern. | No |
| D043 | M003/S02 | arch | Stashed cmdCtx pattern for agent_end | `/issues auto` command handler stashes `ExtensionCommandContext` in module scope; `agent_end` handler (which receives `ExtensionContext` without newSession) reuses it | `newSession()` is only on `ExtensionCommandContext`. GSD auto uses this exact pattern. The stash is cleared on stopAuto(). | No |
| D044 | M003/S02 | convention | Split retry max 3 attempts | Strict mode retries agent-driven split up to 3 times before giving up with error | Prevents infinite retry loops. 3 attempts gives the LLM reasonable chances while bounding resource usage. | Yes — if split quality proves reliable enough for fewer retries |
| D045 | M003/S02 | arch | ExtensionAPI.on() re-added for agent_end | `on()` method added back to `ExtensionAPI` interface for `agent_end` event registration. Supersedes D035's removal. | D035 removed `on()` when tool_result hook was dropped. Auto-flow needs `agent_end` — different event, same API surface. | No |
| D046 | M004 | arch | Remove M003 orchestration state machine | Delete `auto.ts` state machine, `commands/auto.ts`, `gsd_issues_auto` tool, and orchestration `agent_end` handler. Supersedes D041, D042, D043, D044. | gsd-issues should orchestrate on top of GSD, not reimplement GSD's planning/execution loop. The state machine duplicated what GSD already does. Replace with thin `agent_end` hooks that react to GSD's filesystem artifacts (SUMMARY.md, ROADMAP.md). | No |
| D047 | M004 | arch | `/issues` as smart entry mirroring `/gsd` | `/issues` becomes a contextual entry point: no milestone → offer import or fresh start; milestone exists → resume. Mirrors GSD's `showSmartEntry()` pattern. | Users think in work, not milestone IDs. The entry point should figure out context and act, not demand parameters. | No |
| D048 | M004 | arch | Hooks not orchestration for post-milestone actions | PR creation and sizing validation triggered by `agent_end` detecting filesystem state changes (SUMMARY.md, ROADMAP.md), not by a phase-based state machine | pi's `agent_end` event + GSD's filesystem artifacts are the designed integration surface. No parallel orchestration needed. | No |
| D049 | M004 | convention | config.milestone becomes optional | `milestone` field in Config type and validateConfig() changes from required to optional | Milestone IDs are created during scoping, not known at setup time. Existing configs with milestone set still work (resume path). | No |
| D050 | M004 | convention | auto_pr config field defaults to true | New boolean `auto_pr` field in Config, default true. When true, PR is created automatically on milestone completion. When false, user is prompted for confirmation. | Most users want the PR created automatically — confirmation is opt-in friction. | Yes — if users report unwanted PRs |
| D051 | M004/S01 | arch | Module-scoped auto flag for agent_end chaining | `/issues auto` sets a module-scoped `_autoRequested` flag; `agent_end` handler checks this to decide whether to chain to `/gsd auto` after scope completion | Same pattern as M003's stashed context (D043), but simpler — just a boolean flag. `agent_end` receives `ExtensionContext` not `ExtensionCommandContext`, so state must be stashed from the command handler. | No |
| D052 | M004/S01 | arch | Scope completion via CONTEXT.md diffing | Pre-scope milestone scan saved in module state; `agent_end` re-scans and diffs to detect newly created milestones | No new event types needed — just filesystem observation. Same approach as the roadmap suggested. Robust against partial completion (detects whatever was created). | No |
| D053 | M005/S01 | arch | Prompted flow as parallel branch, not shared code with hooks | `_promptedFlowEnabled` flag and prompted `agent_end` branch are separate from `_hooksEnabled` and hooks branch — no code sharing via auto-accept | The hooks path is well-tested and works. Merging the two modes into a single path with a "skip confirmation" toggle risks regressions in auto-mode for minimal DRY benefit. Two parallel branches in `agent_end` are simpler to reason about. | Yes — if a third mode appears |
| D054 | M005/S01 | arch | Mark prompted before sending, not on confirmation | `markSynced()`/`markPrd()` called when the prompt is sent, not when the user confirms | The extension can't await sendMessage responses (fire-and-forget). Marking on send prevents re-prompting across agent_end cycles. If the user says "skip", the milestone stays un-synced but won't be re-prompted — acceptable because `/issues sync` is always available. | Yes — if pi adds response callbacks for sendMessage |
